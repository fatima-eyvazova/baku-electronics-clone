const initialState = {
    basket: [],
    totalCost: 0,
    totalDiscount: 0,
};

export const basketReducer = (state = initialState, action) => {
    switch (action.type) {

    }
    return state;
};
